---
description: Contexto del portfolio Angular, stack, comandos y no-objetivos
alwaysApply: true
---

# Portfolio Miguel Gutierrez

Portfolio personal en Angular 22 (TypeScript + Sass), desplegado como sitio estático en Firebase Hosting. Contenido en español e inglés: perfil, proyectos, experiencia y cursos.

## Stack actual

- Angular 22, RxJS 7, application builder — **sin Bootstrap** (tokens CSS + Sass propios)
- Node.js >= 24.15 (`.nvmrc` → `24`)
- Sin scripts globales de terceros minificados en `angular.json` `scripts` (histórico: Bootstrap JS → sourcemap crash / exit 134)
- Navbar collapse en Angular; output: `dist/.../browser`
- UI Phase 2: **Desktop + tablet L/P + mobile** + **project-detail all breakpoints** (`/projects/:id`, Figma `4:678` / `4:1170` / `4:1019` / `4:866`) + **404 all breakpoints** (`/not-found`, Figma `21:4` / `23:80` / `23:5` / `21:89`)
- Breakpoints CSS: landscape `max-width: 1279px`, portrait `max-width: 1023px`, mobile `max-width: 767px`
- Estructura: `models/`, `content/`, `services/` (incl. Sanity), `components/home|shared|project-detail|not-found`, `studio/`, `assets/`, `environments/`
- Contenido: `ContentService` → Sanity runtime (opcional) + fallback local; idioma/sección en `PreferencesService` (`localStorage`)
- Modelo: `brandHandle`, profile `role`/`pitch`/`focusAreas`, project `technologies` + `detail` (summary/meta/body/features/gallery) + chrome en `PortfolioContent.ui`; contactos hero: GitHub, LinkedIn, phone (`tel:`), WhatsApp (`wa.me`)
- Validación social: `http(s)` + `tel:` / `mailto:` en `content-validator.ts`

## Comandos

- `nvm use` → `npm install` → `npm start` — desarrollo
- `npm run lint` — ESLint (`angular-eslint`)
- `npm test` — Vitest (`ng test --watch=false --coverage`); `npm run test:watch` en watch
- `npm run build` — producción
- `npm run ci` — **validación principal:** lint + test + build de producción
- `npm run lighthouse` — Lighthouse CI a11y sobre `dist/` (después de build); GitHub Actions lo corre en `ci.yml` (`lighthouserc.cjs`, accessibility ≥ 0.9)
- `npm run studio:install` / `npm run studio` — Sanity Studio
- CI: GitHub Actions (`.github/workflows/`) corre `npm run ci` (+ Lighthouse en `ci.yml`) antes de artifact/deploy; secret `FIREBASE_SERVICE_ACCOUNT` para deploy
- Deploy: `npm run build` luego `npx firebase-tools deploy --only hosting`
- **No hay** suite e2e; no añadir Playwright/Cypress salvo que lo pida el usuario

## Principios permanentes

- Mantener el sitio estático, barato de operar y sin backend propio salvo necesidad clara
- Angular a largo plazo; modernizar de forma incremental
- One-pager con anclas (`/#projects`, etc.); contacto solo en footer, no en nav/burger; no tabs que reemplacen contenido
- Navbar **Login** → `environment.adminLoginUrl` (app admin externa + proxy; vacío = oculto); sin tokens de escritura en el portfolio
- Orden: stack ✓ → modelo tipado ✓ → UI ✓ → CMS slice 1+2 ✓ → hardening SEO ✓ → admin (`docs/admin-app-brief.md`) → data → **calidad (ESLint + Vitest → CI) ✓**
- CMS = Sanity CDN read-only + fallback local por colección; schemas en `/studio`; `ui` local
- SEO: `SeoService`, `robots.txt` / `sitemap.xml`, `environment.siteUrl`; headers en `firebase.json`
- Handoff admin: `docs/admin-app-brief.md` (sibling `cv-admin-panel`)
- Tras **cualquier** cambio, sincronizar o revisar `CLAUDE.md`, `.github/copilot-instructions.md` y `.cursor/rules/`

Consultar `docs/improvement-plan.md` antes de cambios de arquitectura o stack.

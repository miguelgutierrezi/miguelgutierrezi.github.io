---
description: CI/CD con GitHub Actions y Firebase Hosting
globs: .github/workflows/**/*,firebase.json,.firebaserc
alwaysApply: false
---

# Deploy y CI

## Target

- Firebase Hosting, project `miguel-angel-gutierrez-ibague`
- Public URL (default): `https://miguel-angel-gutierrez-ibague.web.app` (`environment.siteUrl`)
- Public dir: `dist/personal-presentation-miguel-gutierrez/browser`
- SPA rewrite a `index.html` (static files like `robots.txt` / `sitemap.xml` are served first)
- Headers: `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`; long-cache for hashed JS/CSS; `no-cache` for `index.html`

## GitHub Actions (CircleCI eliminado)

- `ci.yml` — push/PR → `npm ci` + `npm run ci` (lint + Vitest + production build) + Lighthouse a11y (`npm run lighthouse`)
- `deploy.yml` — push `master` → `npm run ci` + live channel
- `firebase-hosting-pull-request.yml` — PR → `npm run ci` + preview channel

Secret requerido: `FIREBASE_SERVICE_ACCOUNT` (JSON de service account).

## Validación

- Gate local/PR core: `npm run ci` = `npm run lint` + `npm test` + `npm run build`
- Gate a11y en Actions: `npm run lighthouse` (`lighthouserc.cjs`) — accessibility ≥ 0.9 en `/`, `/projects/nodejs-scheduler-back`, `/not-found`
- Tests: Vitest (`@angular/build:unit-test`). Lint: `angular-eslint`. No hay e2e.

## Manual

`npm run build` luego `npx firebase-tools deploy --only hosting`
